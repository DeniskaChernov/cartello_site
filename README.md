# Cartello — лендинг автоуслуг

React + Vite + Tailwind. Сборка: `npm run build`, локально: `npm run dev`.

Переменные окружения для API заявок — см. `.env.example`. Бэкенд (Deno): каталог `server/`, деплой через `Dockerfile`.
