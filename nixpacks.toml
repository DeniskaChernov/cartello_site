# Статический фронт (Vite). Не используйте корневой Dockerfile для этого сервиса —
# иначе Railway поднимет только API и на / будет 404.
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
